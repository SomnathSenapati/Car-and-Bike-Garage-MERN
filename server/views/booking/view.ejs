<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body class="container mt-5">
  <h2 class="mb-4">Invoice - <%= bill.invoiceNumber %></h2>

  <p><strong>Date:</strong> <%= bill.date.toDateString() %></p>
  <p><strong>Customer Name:</strong> <%= bill.customer?.name || "N/A" %></p>
  <p><strong>Mechanic Name:</strong> <%= bill.mechanic?.name || "N/A" %></p>

  <h4 class="mt-4">Services :-</h4>
  <ul>
    <% bill.services.forEach(service => { %>
      <li><%= service.name %> - ₹<%= service.price %></li>
    <% }) %>
  </ul>

  <h3 class="mt-3">Total: ₹<%= bill.amount %></h3>

  <hr>
  <button onclick="window.print()" class="btn btn-primary">Print Invoice</button>
</body>
</html>
